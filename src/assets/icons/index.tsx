import RefreshIcon from './RefreshIcon';

export {
  RefreshIcon,
};

export interface IconProps {
  size?: number | string;
  color?: string;
  strokeWidth?: number;
  className?: string;
  title?: string;
  ariaLabel?: string;
}